<h1>カテゴリ一覧</h1>
  <table class="table table-hover">
    <tbody>
      <% @genres.each do |genre| %>
        <tr>
          <td><%= link_to genre.name, items_path(genre_id: genre.id) %></td>
          <td><%= genre.created_at %>作成</td>
        </tr>
      <% end %>
    </tbody>
  </table>